# 033. Hardening — Final Integration & ALL DONE 마커

## Goal
Ralph Loop 최종 검수. 전체 빌드/테스트/문서/커버리지 게이트 통과 확인 후 `docs/ralph/STATUS.md` 를 `ALL DONE` 으로 교체한다.

## Prerequisites
- 032: hardening-prod-profile-actuator-security

## Acceptance Criteria
- [ ] `./gradlew clean build asciidoctor jacocoTestCoverageVerification` 그린 (한 명령).
- [ ] JaCoCo INSTRUCTION ratio ≥ 0.80 (게이트 실측).
- [ ] `build/docs/asciidoc/index.html` 존재 + 26개 엔드포인트 모두 include 됨 확인.
- [ ] `build/generated-snippets/` 아래 26개 엔드포인트 디렉터리 모두 존재 확인 (Health 포함이면 27).
- [ ] `docs/ralph/PROGRESS.md` 의 ID=000~032 모든 row 가 `DONE` + 7자리 SHA 보유.
- [ ] 본 task(033) 완료 커밋 직전, `docs/ralph/STATUS.md` 파일을 정확히 `ALL DONE\n` (단일 라인 + LF 1개) 로 교체.
- [ ] 마지막 커밋에 PROGRESS.md row 033 = DONE + 본 커밋 SHA 갱신 포함.
- [ ] Ralph Loop 가 본 iteration 종료 후 `Read STATUS.md` → 내용 `ALL DONE` 확인 → 루프 종료.

## TDD Entry Points
- 신규 테스트 없음 (전체 통합 검증). 단, `EchoBackApplicationFinalSmokeTest::contextLoadsAcrossAllProfiles` 정도의 가벼운 통합 smoke 1건은 추가 권장.

## Files
- 신규(선택): `src/test/java/com/capstoneecho/echo_back/EchoBackApplicationFinalSmokeTest.java`
- 수정: `docs/ralph/STATUS.md` (`IN PROGRESS` → `ALL DONE`)
- 수정: `docs/ralph/PROGRESS.md` (row 033 DONE + SHA)

## Commit Message Template
`chore(release): final integration + Ralph Loop 종료 (ALL DONE)`
