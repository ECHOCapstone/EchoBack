# 008. Member — MemberService (프로필, 닉네임)

## Goal
인증된 사용자의 프로필 조회와 닉네임 변경을 담당하는 `MemberService` 를 도입한다. 향후 Phase 4 완료 보상(`awardCompletionRewards`) 도 같은 서비스에 위치하지만 그 메서드는 task 025 에서 추가.

## Prerequisites
- 007: member-user-entity-repository

## Acceptance Criteria
- [ ] `com.capstoneecho.echo_back.member.service.MemberService` (`@Service`, `@Transactional(readOnly=true)`).
- [ ] `findMe(userId): MemberProfileResponse` — 미존재 시 `BusinessException(USER_NOT_FOUND)`.
- [ ] `updateNickname(userId, request): MemberProfileResponse` (`@Transactional`) — `@NotBlank @Size(max=20)` 검증 실패 시 `BusinessException(VALIDATION_FAILED)`.
- [ ] DTO: `MemberProfileResponse(id, username, email, nickname, streak, exp, lastStudyAt)`, `NicknameUpdateRequest(nickname)`.
- [ ] `MemberServiceTest` (`@SpringBootTest` + `@Transactional` + `@ActiveProfiles("test")`) 가 정상 + USER_NOT_FOUND + VALIDATION_FAILED 케이스 그린.
- [ ] `./gradlew test --tests MemberServiceTest` 그린.

## TDD Entry Points
- `com.capstoneecho.echo_back.member.service.MemberServiceTest::findMeReturnsProfile`
- `MemberServiceTest::findMeUnknownIdThrowsUserNotFound`
- `MemberServiceTest::updateNicknameRejectsBlank`

## Files
- 신규: `src/main/java/com/capstoneecho/echo_back/member/service/MemberService.java`
- 신규: `src/main/java/com/capstoneecho/echo_back/member/dto/MemberProfileResponse.java`
- 신규: `src/main/java/com/capstoneecho/echo_back/member/dto/NicknameUpdateRequest.java`
- 신규: `src/test/java/com/capstoneecho/echo_back/member/service/MemberServiceTest.java`

## Commit Message Template
`feat(member): MemberService 프로필 조회/닉네임 변경 추가`
