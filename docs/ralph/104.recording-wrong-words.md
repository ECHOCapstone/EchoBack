# 104. Recording — `wrongWords` 를 `WrongWord[]` 로 (D10)

## Goal

FRONT_API_SPEC §12 `WrongWord` 정렬: `RecordingResult.wrongWords` 가 `{ word: string, index: number }[]` 형태여야 한다. 현재 backend 는 `List<String>` 으로 직렬화 — 프론트가 0-based target-word index 를 잃는다. 본 task 가 typed DTO `WrongWord(word, index)` 를 도입하고 LLM/rule-based 응답 구성을 갱신한다.

## Prerequisites

- 100 (envelope-and-error-shape).

## Acceptance Criteria

- [ ] `src/main/java/.../pronunciation/recording/dto/WrongWord.java` (신규): record `WrongWord(String word, int index)`.
- [ ] `pronunciation/recording/dto/RecordingUploadResponse.java`:
  - 필드 `List<String> wrongWords` → `List<WrongWord> wrongWords`.
- [ ] `pronunciation/recording/service/RecordingService.buildOutcome` (또는 `LlmClient.summarizeRecording` 의 `RecordingGuidance.wrongWords` 필드):
  - 결과 타입 `List<WrongWord>` 로 변경.
  - rule-based 폴백: `errors.canonicalIndex` + `targetText` 단어 경계 역산으로 `(word, index)` 계산.
  - 빈 결과는 `List.of()` (직렬화 시 `[]`).
- [ ] `pronunciation/recording/entity/Recording.wrongWordsJson` 필드 (JSON 직렬화 캐시):
  - JSON 형태가 `[{"word":"...","index":0},...]` 로 저장. 역직렬화 시 `WrongWord[]` 로 디코딩.
- [ ] `RecordingResponse.from(Recording)`:
  - `wrongWordsJson` 을 `WrongWord[]` 로 역직렬화. NULL 또는 실패 시 `[]` 폴백 (WARN 로그).
- [ ] `pronunciation/recording/controller/RecordingControllerTest.java`:
  - 응답 assertion: `$.data.wrongWords[0].word` exists, `$.data.wrongWords[0].index` exists (script-flow with-error 케이스).
  - perfect 케이스: `$.data.wrongWords` empty array.
- [ ] `./gradlew compileJava compileTestJava` 그린.
- [ ] **pre-commit 훅 통과**.
- [ ] `rg -n "List<String>\s+wrongWords" src/main` 0 hits.

## TDD Entry Points

- `pronunciation.recording.controller.RecordingControllerTest` 의 script-flow with-error 골든 → `$.data.wrongWords[0].word` / `$.data.wrongWords[0].index` 검증.
- `pronunciation.recording.entity.RecordingTest` (만약 있으면) → `wrongWordsJson` 역직렬화 round-trip 검증.

## Files

- 신규: `src/main/java/com/capstoneecho/echo_back/pronunciation/recording/dto/WrongWord.java`
- 수정: `src/main/java/com/capstoneecho/echo_back/pronunciation/recording/dto/RecordingUploadResponse.java`
- 수정: `src/main/java/com/capstoneecho/echo_back/pronunciation/recording/service/RecordingService.java` (buildOutcome 또는 동등 경로)
- 수정: `src/main/java/com/capstoneecho/echo_back/external/llm/LlmClient.java` (`RecordingGuidance.wrongWords` 타입)
- 수정: `src/main/java/com/capstoneecho/echo_back/external/llm/RuleBasedLlmFeedbackGenerator.java` (rule-based 폴백)
- 수정: `src/test/java/com/capstoneecho/echo_back/pronunciation/recording/controller/RecordingControllerTest.java`

## References

- `docs/IMPLEMENTATION_PLAN.md` §2 (D10), §3 (Task 104)
- `docs/API_SPEC_REFINED.md` §5.3, §5.15
- `docs/COMPONENTS_REFINED.md` §3.3.1
- `docs/FRONT_API_SPEC.md` §12 `WrongWord` + `RecordingResult.wrongWords`
- Codex 2026-05-13 review finding #6

## Commit Message Template

`feat(api): change RecordingResult.wrongWords to WrongWord[] (Codex D10)`
