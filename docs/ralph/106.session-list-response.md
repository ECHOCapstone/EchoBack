# 106. Session — `GET /api/sessions` 응답을 전체 `Session[]` 으로 (D11)

## Goal

FRONT_API_SPEC §6 정렬: `GET /api/sessions` 가 전체 `Session[]` (포함 `scriptText` + `sentences`) 를 반환해야 한다. 현재 backend 는 `SessionSummaryResponse[]` (id/title/favorite/timestamps 만) 를 반환 — 프론트가 list 화면에서 sentence 데이터를 잃는다.

본 task 의 정책 결정 (locked 2026-05-13): backend 를 FRONT 에 정렬한다 (spec 수정 아님). perf 영향 (list 시 sentences eager fetch) 은 post-merge 재평가.

## Prerequisites

- 100 (envelope-and-error-shape).

## Acceptance Criteria

- [ ] `learning/session/controller/SessionController.java`:
  - `list(@CurrentUser JwtPrincipal principal)` 반환 타입 → `ApiResponse<List<SessionDetailResponse>>` (또는 spec-shape DTO 이름).
- [ ] `learning/session/service/SessionService.listMine(Long userId)`:
  - 반환 타입을 detail DTO 로 변경. 기존 detail mapping helper (`SessionResponse.from(Session)` 등) 재사용.
- [ ] `learning/session/repository/SessionRepository`:
  - `findByUser_IdOrderByFavoriteDescUpdatedAtDesc` 메서드에 `@EntityGraph(attributePaths = "sentences")` 추가 — N+1 회피.
- [ ] `learning/session/dto/SessionSummaryResponse.java` (있으면): 다른 사용처가 없으면 삭제. 다른 사용처가 있다면 그대로 두되 list endpoint 가 사용하지 않음을 확인.
- [ ] `learning/session/controller/SessionControllerTest.java`:
  - `list` 응답 assertion: `$.data[0].id` / `$.data[0].title` / `$.data[0].scriptText` / `$.data[0].favorite` / `$.data[0].sentences[0].text` / `$.data[0].sentences[0].sentenceIndex` / `$.data[0].createdAt` / `$.data[0].updatedAt` 모두 검증.
  - empty 케이스: `$.data` empty array.
  - cross-user 격리: A 토큰으로 list → B 의 세션이 결과에 없음.
- [ ] `./gradlew compileJava compileTestJava` 그린.
- [ ] **pre-commit 훅 통과**.

## TDD Entry Points

- `learning.session.controller.SessionControllerTest#list` (응답에 sentences 까지 검증).

## Files

- 수정: `src/main/java/com/capstoneecho/echo_back/learning/session/controller/SessionController.java`
- 수정: `src/main/java/com/capstoneecho/echo_back/learning/session/service/SessionService.java`
- 수정: `src/main/java/com/capstoneecho/echo_back/learning/session/repository/SessionRepository.java` (`@EntityGraph`)
- 수정 (또는 삭제): `src/main/java/com/capstoneecho/echo_back/learning/session/dto/SessionSummaryResponse.java`
- 수정: `src/test/java/com/capstoneecho/echo_back/learning/session/controller/SessionControllerTest.java`

## References

- `docs/IMPLEMENTATION_PLAN.md` §2 (D11), §3 (Task 106)
- `docs/API_SPEC_REFINED.md` §4.7
- `docs/COMPONENTS_REFINED.md` §3.2.3
- `docs/API_TEST_PLAN.md` §3.13
- `docs/FRONT_API_SPEC.md` §6 + §12 `Session`
- Codex 2026-05-13 review finding #7

## Commit Message Template

`feat(api): return full Session[] from GET /api/sessions (Codex D11)`
