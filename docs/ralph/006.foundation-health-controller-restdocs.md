# 006. Foundation — HealthController + REST Docs 파이프라인

## Goal
M1 (Foundation Ready) 검수 기준인 `/api/health` 엔드포인트와 Spring REST Docs 스니펫 생성 파이프라인을 함께 구축한다.

## Prerequisites
- 004: foundation-security-config

## Acceptance Criteria
- [ ] `com.capstoneecho.echo_back.system.HealthController` 가 `GET /api/health` 를 public 으로 노출, `ApiResponse<Map<String,Object>>` 봉투에 `{status:"UP", service:"echo-app-backend", timestamp:<ISO-8601 UTC>}` 데이터.
- [ ] `AbstractControllerIntegrationTest` 베이스 클래스 도입: `@SpringBootTest` + `@AutoConfigureMockMvc` + `@AutoConfigureRestDocs(outputDir="build/generated-snippets")` + `@ActiveProfiles("test")`.
- [ ] `HealthControllerTest extends AbstractControllerIntegrationTest` 가 200 + 봉투 JSON 검증 + `MockMvcRestDocumentation.document("system/get-health", ...)` 호출.
- [ ] `./gradlew test --tests HealthControllerTest` 그린.
- [ ] 빌드 후 `build/generated-snippets/system/get-health/` 디렉터리에 스니펫 6+개 (http-request, http-response, curl-request 등) 생성 확인.

## TDD Entry Points
- `com.capstoneecho.echo_back.system.HealthControllerTest::returnsUpEnvelopeAndDocumentsSnippets`

## Files
- 신규: `src/main/java/com/capstoneecho/echo_back/system/HealthController.java`
- 신규: `src/test/java/com/capstoneecho/echo_back/support/AbstractControllerIntegrationTest.java`
- 신규: `src/test/java/com/capstoneecho/echo_back/system/HealthControllerTest.java`

## Commit Message Template
`feat(system): HealthController 와 REST Docs 파이프라인 부트스트랩`
