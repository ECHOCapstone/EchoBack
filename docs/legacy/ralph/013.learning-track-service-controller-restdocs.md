# 013. Learning — TrackService / TrackController + REST Docs (2 엔드포인트)

## Goal
트랙 목록과 트랙+챕터 상세 두 엔드포인트를 노출한다.

## Prerequisites
- 012: learning-track-script-step-entities
- 011: member-controllers-restdocs

## Acceptance Criteria
- [ ] `TrackService` (`@Service`, `@Transactional(readOnly=true)`):
  - `listTracks(): List<TrackSummaryResponse>` — `displayOrder` 오름차순.
  - `getTrack(trackId): TrackDetailResponse` — 미존재 시 `BusinessException(TRACK_NOT_FOUND)`. 챕터(=`chapterOrder` 정의된 스크립트들) 묶음 포함.
- [ ] `TrackController` (`@RequestMapping("/api/tracks")`, JWT 필요):
  - `GET /` — 200 + 봉투 + List<TrackSummaryResponse>.
  - `GET /{trackId}` — 200 + 봉투 + TrackDetailResponse. 미존재 404.
- [ ] DTO: `TrackSummaryResponse(id, title, description, displayOrder)`, `TrackDetailResponse(id, title, description, chapters: List<ChapterResponse>)`, `ChapterResponse(scriptId, title, chapterOrder, difficulty, preset)`.
- [ ] `TrackControllerTest` 가 2 엔드포인트 성공 + 미존재 404 + 미인증 401 + REST Docs 스니펫(`tracks/list`, `tracks/get`) 그린.
- [ ] `./gradlew test --tests TrackControllerTest` 그린.

## TDD Entry Points
- `com.capstoneecho.echo_back.learning.track.controller.TrackControllerTest::listReturnsByDisplayOrder`
- `TrackControllerTest::getUnknownTrackReturns404TrackNotFound`

## Files
- 신규: `src/main/java/com/capstoneecho/echo_back/learning/track/service/TrackService.java`
- 신규: `src/main/java/com/capstoneecho/echo_back/learning/track/controller/TrackController.java`
- 신규: `src/main/java/com/capstoneecho/echo_back/learning/track/dto/TrackSummaryResponse.java`
- 신규: `src/main/java/com/capstoneecho/echo_back/learning/track/dto/TrackDetailResponse.java`
- 신규: `src/main/java/com/capstoneecho/echo_back/learning/track/dto/ChapterResponse.java`
- 신규: `src/test/java/com/capstoneecho/echo_back/learning/track/controller/TrackControllerTest.java`

## Commit Message Template
`feat(learning): TrackController + REST Docs 2건`
