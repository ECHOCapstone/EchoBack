# 031. Hardening — JaCoCo 80%+ gate + asciidoctor 통합

## Goal
JaCoCo 커버리지 게이트(80%)를 빌드에 강제하고, REST Docs 스니펫을 단일 Asciidoc 산출물로 통합한다.

## Prerequisites
- 030: ranking-service-controller-restdocs
- 027: tts-client-service-controller-restdocs

## Acceptance Criteria
- [ ] `build.gradle` 에 `jacoco` 플러그인 적용 + `jacocoTestReport` 후속에 `jacocoTestCoverageVerification` 설정:
  - rule: `INSTRUCTION` minimum ratio `0.80`.
  - 제외: `**/dto/**`, `**/entity/**` 의 단순 getter/equals/hashCode (선택), Lombok 생성 코드.
- [ ] `asciidoctor` 태스크가 `build/generated-snippets` → `src/docs/asciidoc/index.adoc` (include 매크로) → `build/docs/asciidoc/index.html`.
- [ ] `src/docs/asciidoc/index.adoc` 시드 작성: 5 도메인 섹션(Member/Learning/Pronunciation/Stats/System) + 각 엔드포인트 include.
- [ ] `./gradlew clean test jacocoTestReport jacocoTestCoverageVerification asciidoctor` 그린.
- [ ] `build/docs/asciidoc/index.html` 파일 생성 확인.

## TDD Entry Points
- 본 task 는 빌드 인프라 변경 — 단위 테스트 신규 없음. 검증은 `./gradlew` 명령으로.

## Files
- 수정: `build.gradle`
- 신규: `src/docs/asciidoc/index.adoc`

## Commit Message Template
`chore(release): JaCoCo 80% gate + Asciidoctor 통합`
